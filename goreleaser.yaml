version: 2
project_name: vexd

release:
  mode: replace
  draft: false
  prerelease: auto

builds:
  - id: vexd
    main: ./cmd/vexd
    binary: vexd
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - windows
      - darwin
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X main.version={{.Version}}
      - -X main.commit={{.Commit}}
      - -X main.date={{.Date}}

archives:
  - id: archive
    ids: [vexd]
    name_template: '{{ .ProjectName }}_{{ .Os }}_{{ .Arch }}{{ with .Arm }}v{{ . }}{{ end }}{{ with .Mips }}_{{ . }}{{ end }}{{ if not (eq .Amd64 "v1") }}{{ .Amd64 }}{{ end }}'
    format_overrides:
      - goos: windows
        formats: ['zip']
    files:
      - LICENSE
      - README.md

nfpms:
  - id: vexd
    package_name: vexd
    file_name_template: '{{ .PackageName }}_{{ .Os }}_{{ .Arch }}{{ with .Arm }}v{{ . }}{{ end }}{{ with .Mips }}_{{ . }}{{ end }}{{ if not (eq .Amd64 "v1") }}{{ .Amd64 }}{{ end }}'
    ids: [vexd]
    maintainer: "Jairo <jairo.jggc@gmail.com>"
    description: "Daemon de ejecución para despliegues automáticos y rápidos de aplicaciones"
    homepage: "https://github.com/jairoprogramador/vex-engine"
    license: Apache License
    formats:
      - deb
      - rpm
      - apk

homebrew_casks:
  - name: vexd
    ids:
      - archive
    repository:
      owner: jairoprogramador
      name: homebrew-vex-engine
      token: "{{ .Env.HOMEBREW_TAP_TOKEN }}"
    commit_author:
      name: Jairo Galvez
      email: jairo.jggc@gmail.com
    homepage: "https://github.com/jairoprogramador/vex-engine"
    description: "Daemon de ejecución para despliegues automáticos y rápidos de aplicaciones"
    binaries: [vexd]

changelog:
  use: git
  filters:
    exclude:
      - '^docs:'
      - '^test:'
      - '^chore:'
